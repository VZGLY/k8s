apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  labels:
    type: local

spec:
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  volumeName: "pv"
  
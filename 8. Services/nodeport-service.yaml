apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  labels:
    env: test
spec:
  selector:
    env: test
    app: front
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
      